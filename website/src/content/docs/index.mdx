---
title: Quon
description: A Rust quantum compiler toolkit for typed programs, OpenQASM 3 emission, and neutral-atom schedule/resource artifacts.
template: splash
hero:
  title: Quon
  tagline: A Rust quantum compiler toolkit for typed quantum programs, backend-aware lowering, OpenQASM 3 emission, and neutral-atom schedule/resource artifacts.
  actions:
    - text: Choose your path
      link: /getting-started/choose-your-path/
      icon: right-arrow
      variant: primary
    - text: Run the quickstart
      link: /getting-started/quickstart/
    - text: Install Quon
      link: /getting-started/install/
      icon: right-arrow
      variant: minimal
    - text: Read the language guide
      link: /language/introduction/
      icon: right-arrow
      variant: minimal
    - text: View on GitHub
      link: https://github.com/arniber21/quon
      icon: external
      variant: minimal
---

import { Card, CardGrid } from '@astrojs/starlight/components';

<!--
  BELL WALKTHROUGH OWNERSHIP (issue #384):
  This page is a TEASER ONLY. It shows the Bell circuit once, as a motivating
  example, and links out to the canonical owners:
    - /getting-started/quickstart/  — execution + artifact inspection
    - /language/introduction/       — progressive concept explanation
    - /cookbook/bell/               — verified algorithm/artifact analysis
  Do NOT paste the full run block, type annotations, MLIR excerpts, or Aer
  output here. The audit script (website/scripts/audit-bell-walkthrough.sh)
  enforces this.
-->

## A typed quantum compiler

Quon is a typed quantum compiler. Programs carry depth bounds, Clifford
classifications, and linear resource ownership in their types — not as comments,
not as runtime checks, as types the compiler verifies before a single gate is
lowered.

A circuit is a value with a four-parameter type: `Circuit<n, m, d, C>`. The
type says *this transformation takes `n` qubits in, produces `m` qubits out,
completes in `d` steps, and belongs to the Clifford class `C`*. The typechecker
enforces every one of those claims. Compose two circuits with `|>` and the
depths add; run them in `par` and the depth is the maximum. Break the depth
budget, mis-count the qubits, use a qubit twice, or forget to measure — and the
program does not compile.

This is what a typed Bell pair looks like:

```kotlin
fn bell_state(): Circuit<2, 2, 2, Clifford> = circuit {
    H @0 |> CNOT @(0, 1)
}
```

The return type `Circuit<2, 2, 2, Clifford>` is a contract: two qubits in,
two out, depth exactly two, Clifford class — all four checked before a single
gate is lowered. The [language guide](/language/introduction/) unpacks the
types from first principles; the [cookbook](/cookbook/bell/) verifies the
circuit end to end.

## Choose your path

Route yourself by what you want to do. Each path states its prerequisite,
expected outcome, and next page on the
[orientation page](/getting-started/choose-your-path/).

The docs are organized in three depths, each linking to the next in both
directions: **Language guide** *(concepts)* explains what a `.qn` program
means and why; **Reference** *(contract)* is stable lookup of what each
pipeline stage and `quonc` flag guarantees; **Architecture** *(rationale)*
explains where each stage lives in the compiler source and why the ADRs
shaped it that way.

<CardGrid>
	<Card title="First-time learner" icon="book-open">
		Learn typed quantum programming from first principles — no prerequisite.
		[Start the learning track ->](/getting-started/choose-your-path/#first-time-learner)
	</Card>
	<Card title="Algorithm researcher" icon="puzzle">
		Run complete, Aer-verified algorithms with checked resource bounds.
		[Open the cookbook ->](/getting-started/choose-your-path/#algorithm-researcher)
	</Card>
	<Card title="Backend / neutral-atom user" icon="setting">
		Compile to OpenQASM 3 or a neutral-atom schedule and resource report.
		[Target a backend ->](/getting-started/choose-your-path/#backend--neutral-atom-user)
	<Card title="Install Quon" icon="setting">
		Set up the Rust toolchain, LLVM/MLIR 22, and Z3 with Devbox or system
		packages, then build the `quonc` compiler.
		[Install ->](/getting-started/install/)
	</Card>
	<Card title="Run the quickstart" icon="rocket">
		Compile a checked-in Bell pair to OpenQASM 3. Aer sampling and
		neutral-atom emission are optional follow-ons with their own
		prerequisites.
		[Run it ->](/getting-started/quickstart/)
	</Card>
	<Card title="Your second program" icon="right-arrow">
		Teleportation with `QReg` destructuring, the Quantum Monad,
		measurement, and classical feed-forward control.
		[Read it ->](/getting-started/second-program/)
	</Card>
	<Card title="Learn the language" icon="book-open">
		*(concepts)* Linear qubit types, the `Circuit<n, m, d, C>` contract,
		the Quantum Monad, classical control, and parametric circuits —
		explained from first principles.
		[Start the guide ->](/language/introduction/)
	</Card>
	<Card title="Read the architecture" icon="setting">
		The compiler pipeline from source text to backend artifact: parsing,
		typechecking, MLIR lowering, optimization passes, and target-specific
		emission.
		[See the pipeline ->](/architecture/compiler-internals/)
	</Card>
	<Card title="Language author" icon="wrench">
		Extend the compiler or build the LSP, formatter, and linter.
		[Read the architecture ->](/getting-started/choose-your-path/#language-author)
	</Card>
	<Card title="Contributor" icon="github">
		Improve the codebase or docs against the pre-PR gate.
		[Set up a checkout ->](/getting-started/choose-your-path/#contributor)
	</Card>
</CardGrid>

## One source, target-specific outputs

The same typed program lowers to different backends without source changes. A
fixed gate-model target receives OpenQASM 3; a reconfigurable neutral-atom
target receives a movement schedule and a resource report. The types survive the
entire pipeline — depth bounds and Clifford classifications inform optimization
passes, not just the frontend.

Compile the Bell program for a fixed gate-model target:

```bash
cargo run -p quonc -- test/verify/bell.qn --emit-qasm
```

Compile it for a reconfigurable neutral-atom target:

```bash
cargo run -p quonc -- test/na/bell.qn \
  --target targets/neutral_atom/generic_rna_v0.json \
  --emit-na-schedule \
  --emit-resource-report
```

The [quickstart](/getting-started/quickstart/) walks both targets end to end —
compilation, Aer sampling, and artifact inspection.
