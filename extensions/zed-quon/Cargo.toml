[package]
name = "zed-quon"
version = "0.0.1"
edition = "2021"
publish = false
license = "Apache-2.0"

# Isolated from the monorepo workspace (Zed builds this crate separately).
[workspace]

[lib]
crate-type = ["cdylib"]

[dependencies]
zed_extension_api = "0.7.0"
