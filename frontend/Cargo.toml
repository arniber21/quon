[package]
name    = "frontend"
version = "0.1.0"
edition = "2024"

[dependencies]
chumsky   = { workspace = true }
z3        = { workspace = true }
thiserror = { workspace = true }
ariadne   = { workspace = true }
anyhow    = { workspace = true }
quon_core   = { path = "../quon_core" } # shared DepthExpr (MLIR-free)
mlir_bridge = { path = "../mlir_bridge" }
melior      = { workspace = true }

[dev-dependencies]
proptest    = { workspace = true }
insta       = { workspace = true }
melior      = { workspace = true }
mlir_bridge = { path = "../mlir_bridge" }
backend     = { path = "../backend" }
# Shared with the fuzz crate: the proptest roundtrip test drives the same Unstructured-based
# generator (frontend/fuzz/src/gen.rs) via random bytes.
arbitrary   = { workspace = true }

[[example]]
name = "circ_lower"
path = "examples/circ_lower.rs"
