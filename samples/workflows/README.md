# Workflow samples

Developer-workflow narratives: edit -> verify loops, reading a pass dump,
debugging a failing compile ("failure clinic") — how you actually work with
`quonc` day to day, not just what a program computes.

## Status

Owned by pack **#188**. This directory currently holds only a placeholder
seed (`edit_verify_loop.qn`) proving the category has a valid catalog row;
#188 will replace/extend it with the real edit->verify loop, pass-dump, and
failure-clinic walkthroughs.

## Seeds

| Catalog id | Path | Notes |
| --- | --- | --- |
| `workflows/edit-verify-loop-stub` | [`edit_verify_loop.qn`](./edit_verify_loop.qn) | Placeholder — do not extend without coordinating with #188 |

## Do not extend this stub

Coordinate with #188 before adding real narrative content here — one
canonical `.qn` per story (see [`../CONTRIBUTING.md`](../CONTRIBUTING.md)).
