# Visualization samples

Programs and stress artifacts whose story is "here is what the schedule /
interaction graph / resource report should look like" — goldens for the
NA schedule and graph visualizers (`python/visualize_na_schedule.py` and
friends).

## Status

Owned by pack **#189**. This directory currently holds only a placeholder
seed (`schedule_preview.qn`) proving the category has a valid catalog row;
#189 will replace/extend it with real stress artifacts and "what you should
see" goldens.

## Seeds

| Catalog id | Path | Notes |
| --- | --- | --- |
| `visualization/schedule-preview-stub` | [`schedule_preview.qn`](./schedule_preview.qn) | Placeholder — do not extend without coordinating with #189 |

## Do not extend this stub

Coordinate with #189 before adding real narrative content here — one
canonical `.qn` per story (see [`../CONTRIBUTING.md`](../CONTRIBUTING.md)).
