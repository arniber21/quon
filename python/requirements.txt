# Qiskit Aer verification bridge dependencies (issue #29).
# `qiskit.qasm3.loads` requires the separate importer package on Qiskit >= 1.0.
# Also used by python/noisy_fidelity.py (issue #117).
qiskit>=1.0
qiskit-aer>=0.14
qiskit-qasm3-import>=0.5
# QEC Stim/Sinter evaluation path (ADR-0022 / #255, #253).
# pymatching is the default Sinter decoder used by python/quon_qec_sinter.py.
stim
sinter
pymatching
# Local test runner: `pytest python -q` collects the same unittest suites CI
# runs one-by-one via `python -m unittest` in `just ci-rust`.
pytest
# Optional (maintainer-only): regenerate IBM snapshots via --backend
# qiskit-ibm-runtime>=0.20
